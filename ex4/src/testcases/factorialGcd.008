(* Missing LeftParenthesis. *)
MODULE factorialGcd;
	CONST id1 = 123;
	TYPE id2 = ARRAY 10  OF INTEGER ;
	VAR m, n, result: INTEGER;
	 recordid: RECORD
		id3: INTEGER;
	END;	

	PROCEDURE factorial  (n: INTEGER);
	VAR tmp : INTEGER;	
	begin	
		gcd(3, 5);
		read  tmp); (* Missing LeftParenthesis. *)
		
	END factorial;

	PROCEDURE gcd (m , n: INTEGER);
		VAR t, n, m: INTEGER;
	BEGIN
		
		IF (m > 3) THEN
			t := 23
		
		END;
		WHILE (m MOD n # 0) DO
			t := m MOD n; 
			m := n;
			n := t;
		END;
	END gcd;

begin
	READ(n);
	factorial(n); 
	WRITE(result);
	WRITELN;

	READ(m);
	READ(n);
	gcd(m, n);
	WRITE(result);
	WRITELN;

END factorialGcd.